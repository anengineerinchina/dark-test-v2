language: cpp
compiler:
  - gcc
#  - clang
# Install dependencies
# install: make get-deps
install: sudo add-apt-repository -y ppa:ubuntu-toolchain-r/test && sudo apt-get -qq update && sudo apt-get -qq install g++-4.8 && sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.8 90 && export CC=gcc && gcc --version && cd libjl777 && make onetime
# Build and test
script: echo "Start make SuperNET" && make SuperNET && echo "Start make btcd" && make btcd